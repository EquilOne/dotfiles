screencopy {
    custom_picker_binary = hyprland-preview-share-picker
    # Single-user machine: skip permission prompts for screensharing.
    # Set to false if you want per-app permission dialogs instead.
    allow_token_by_default = true
}
