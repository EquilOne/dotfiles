.bak*
.bak/**
# Nested backup files anywhere in the tree (e.g. nvim/lua/plugins/themes.lua.bak,
# bat/rose-pine-moon.tmTheme.bak) that the root `.bak*`/`.bak/**` patterns miss.
**/*.bak
.git
.git/**
**/.git
**/.git/**
nvim/*
!nvim/lua
!nvim/lua/**
!nvim/.gitignore

# Environment files
**/*.env
**/env.local
**/env.*

# Repository documentation (not deployed).
docs/**

# Dead .conf files under the Hyprland 0.56 lua config — omarchy owns the live
# config (~/.config/hypr/*.lua); these vendored .conf copies are obsolete and the
# xdph.conf delta is only a cosmetic comment reorder. Unconditional (all profiles).

{{- if eq (index . "profile") "remote" }}
# Desktop session and presentation state.
.config/waybar
.config/waybar/**
.config/omarchy
.config/omarchy/**
.config/ghostty
.config/ghostty/**
.config/mako
.config/mako/**
.config/gh
.config/gh/**
.config/*/assets
.config/*/assets/**
.config/*/backgrounds
.config/*/backgrounds/**
.config/*/wallpaper*
.config/*/wallpapers
.config/*/wallpapers/**
.config/zsh/.zhistory

# Desktop-only generated completions.
.config/shell/25_completions/03_hyprctl.sh

# Desktop-only theme links and Omarchy-dependent configuration.
.config/btop/btop.conf
.config/btop/themes/symlink_current.theme
.config/eza/symlink_theme.yml
.config/opencode/skills/symlink_omarchy

# Tmux remains in the source tree as a manual fallback, but is not installed remotely.
.config/tmux
.config/tmux/**
.config/nvim/lua/plugins/vim-tmux-navigator.lua
{{- end }}

# Desktop: Omarchy owns these paths and rewrites them on theme switch
# (~/.local/state/omarchy/current/theme), so chezmoi must not manage them —
# the vendored copies in the source are obsolete but kept as mapped fallbacks.
{{- if eq (index . "profile") "desktop" }}
.config/omarchy
.config/omarchy/**
.config/btop
.config/btop/**
.config/jolt
.config/jolt/**
.config/mdt
.config/mdt/**

# Renders empty on desktop; harmless remotely — do not move into the remote gate.
10_remote-fedora-setup.sh
{{- end }}

# OpenCode runtime package manifests — never managed, so a tracked stale copy
# can never downgrade the plugin SDK (e.g. 1.18.31 → 1.18.11).
.config/opencode/package.json
.config/opencode/package-lock.json
